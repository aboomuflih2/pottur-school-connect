# Environment Variables Template for Self-Hosted Supabase on VPS
# Copy this file to .env.production and fill in the actual values
# VPS IP: 63.250.52.6

# =============================================================================
# SUPABASE CONFIGURATION (Self-Hosted on VPS)
# =============================================================================

# Self-hosted Supabase Configuration on VPS
# VPS IP: 63.250.52.6
VITE_SUPABASE_URL="http://63.250.52.6:8000"
VITE_SUPABASE_PUBLISHABLE_KEY="your-self-hosted-anon-key"
VITE_SUPABASE_SERVICE_ROLE_KEY="your-self-hosted-service-role-key"

# Supabase Studio URL (for admin access)
VITE_SUPABASE_STUDIO_URL="http://63.250.52.6:3000"

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Database connection (if needed for direct access)
DATABASE_URL="postgresql://postgres:your-password@63.250.52.6:5432/postgres"
DATABASE_DIRECT_URL="postgresql://postgres:your-password@63.250.52.6:5432/postgres"

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server settings
PORT=3000
NODE_ENV=production

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# JWT Secret (should match your Supabase JWT secret)
JWT_SECRET="your-jwt-secret-key"

# CORS settings
CORS_ORIGIN="http://63.250.52.6:3000,https://your-domain.com"

# =============================================================================
# FILE UPLOAD CONFIGURATION
# =============================================================================

# File upload settings
MAX_FILE_SIZE=10485760
UPLOAD_DIR="./uploads"

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================

# Email settings (if using SMTP)
SMTP_HOST="your-smtp-host"
SMTP_PORT=587
SMTP_USER="your-smtp-user"
SMTP_PASS="your-smtp-password"
SMTP_FROM="noreply@your-domain.com"

# =============================================================================
# DOMAIN AND SSL CONFIGURATION
# =============================================================================

# Domain settings
DOMAIN="your-domain.com"
BASE_URL="https://your-domain.com"

# SSL settings
SSL_CERT_PATH="/path/to/ssl/cert.pem"
SSL_KEY_PATH="/path/to/ssl/private.key"

# =============================================================================
# INSTRUCTIONS
# =============================================================================

# 1. Replace 'your-self-hosted-anon-key' with your actual Supabase anon key
# 2. Replace 'your-self-hosted-service-role-key' with your actual service role key
# 3. Update database password and other credentials
# 4. Configure your domain settings if using a custom domain
# 5. Set up SSL certificates if using HTTPS
# 6. Test the connection using: npm run test:connection

# =============================================================================
# GETTING YOUR SUPABASE KEYS
# =============================================================================

# To get your Supabase keys from your self-hosted instance:
# 1. Access Supabase Studio at: http://63.250.52.6:3000
# 2. Go to Settings > API
# 3. Copy the 'anon public' key for VITE_SUPABASE_PUBLISHABLE_KEY
# 4. Copy the 'service_role' key for VITE_SUPABASE_SERVICE_ROLE_KEY
# 5. The JWT secret can be found in Settings > API > JWT Settings

# =============================================================================
# FIREWALL CONFIGURATION
# =============================================================================

# Make sure these ports are open on your VPS:
# - Port 8000: Supabase API
# - Port 3000: Supabase Studio
# - Port 5432: PostgreSQL (if direct access needed)
# - Port 80/443: Web server (if hosting frontend on same VPS)

# Example firewall commands (Ubuntu/Debian):
# sudo ufw allow 8000
# sudo ufw allow 3000
# sudo ufw allow 5432
# sudo ufw allow 80
# sudo ufw allow 443